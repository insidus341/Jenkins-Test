pipeline {
 agent any
 
 environment {
  TEST_SECRET   = credentials("jenkins-test-secret-1")
 }
  
 stages {
  stage ('Lint') {
   steps {
    echo "HELLO"
   }
  }
  stage ('Lint 22') {
   steps {
    echo "HELLO"
   }
  }
 }
}
